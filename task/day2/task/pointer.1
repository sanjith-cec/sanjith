#include <stdio.h>

int main() {
    // If-Else Example
    int x = 5;
    if (x) {
        printf("True\n");
    } else {
        printf("False\n");
    }

    // Nested If-Else Example
    int temp;
    printf("Enter temperature: ");
    scanf("%d", &temp);

    if (temp <= 0) {
        printf("Temperature status: Freezing\n"); 
    } else if (temp <= 15) {
        printf("Temperature status: Cold\n");
    } else if (temp <= 25) {
        printf("Temperature status: Pleasant\n");
    } else if (temp <= 35) {
        printf("Temperature status: Hot\n");
    } else {
        printf("Temperature status: Very Hot\n");
    }
    
// Switch–Case
// Definition:
// Used for multi-branch decisions on same variable.

    int day = 4;
    switch(day) {
        case 1: printf("Monday"); break;
        case 2: printf("Tuesday"); break;
        case 3: printf("Wednesday"); break;
        case 4: printf("Thrusday"); break;
        case 5: printf("Friday"); break;
        case 6: printf("Saturday"); break;
        case 7: printf("Sunday"); break;
        default: printf("Unknown Day");
    }

// Ternary Operator
// Definition:
// // Short alternative to if–else.
//    1. Find maximum of two numbers:
    

  #include<stdio.h>
#include<string.h>

int main()
{ 
    char pass[50];
    int ok;

    do{ 
        ok=1;
        printf("enter passwowrd: ");
        scanf("%s", pass);

        int len = strlen(pass);
        int up = 0, sp = 0, num =0;
        

        for(int i= 0; i < len; i++)
         {
            if(pass[i] >= 'A' && pass[i] <= 'Z') up = 1;
            else if(pass[i] >= '0' && pass[i] <= '9') num = 1;
            else if(pass[i] == '@' || pass[i] == '#'  || pass[i] == '_') sp = 1;

        } 
        if(len < 8 || !up || !num || !sp)
         ok = 0;
               }
               while(ok == 0);
               printf("strong password accepted!\n");
               return 0;
}

